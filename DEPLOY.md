# 🚀 Инструкция по развертыванию бота онлайн

## 📋 Данные для входа в админ-панель:

```
Логин: botadmin
Пароль: TgBot2026!
```

**⚠️ ВАЖНО:** После развертывания смените пароль в файле `web/app.py`!

---

## Вариант 1: Render.com (Рекомендуется - Бесплатно)

### Шаг 1: Подготовка
1. Зарегистрируйтесь на https://render.com
2. Подключите свой GitHub аккаунт

### Шаг 2: Загрузка кода на GitHub
1. Создайте новый репозиторий на GitHub
2. Загрузите все файлы проекта:
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
git push -u origin main
```

### Шаг 3: Развертывание на Render
1. Войдите на https://render.com
2. Нажмите "New +" → "Web Service"
3. Подключите ваш GitHub репозиторий
4. Настройки:
   - **Name:** telegram-bot-admin (или любое имя)
   - **Environment:** Python 3
   - **Build Command:** `pip install -r requirements_deploy.txt`
   - **Start Command:** `python app.py`
   - **Plan:** Free

5. Нажмите "Create Web Service"

### Шаг 4: Получение URL
После развертывания вы получите URL вида:
```
https://telegram-bot-admin.onrender.com
```

Это и будет ваша админ-панель!

---

## Вариант 2: PythonAnywhere (Бесплатно)

### Шаг 1: Регистрация
1. Зарегистрируйтесь на https://www.pythonanywhere.com
2. Выберите бесплатный план

### Шаг 2: Загрузка файлов
1. Перейдите в "Files"
2. Загрузите все файлы проекта
3. Или используйте консоль:
```bash
git clone https://github.com/ваш-username/ваш-репозиторий.git
```

### Шаг 3: Установка зависимостей
В консоли:
```bash
pip install -r requirements_deploy.txt
```

### Шаг 4: Настройка Web App
1. Перейдите в "Web"
2. "Add a new web app"
3. Выберите Flask
4. Укажите путь к `app.py`

### Шаг 5: Запуск
Ваш сайт будет доступен по адресу:
```
https://ваш-username.pythonanywhere.com
```

---

## Вариант 3: Railway.app (Бесплатно с ограничениями)

### Шаг 1: Регистрация
1. Зарегистрируйтесь на https://railway.app
2. Подключите GitHub

### Шаг 2: Развертывание
1. "New Project" → "Deploy from GitHub repo"
2. Выберите ваш репозиторий
3. Railway автоматически определит Python проект

### Шаг 3: Настройки
Railway автоматически:
- Установит зависимости из `requirements_deploy.txt`
- Запустит приложение через `Procfile`

### Шаг 4: Получение URL
После развертывания получите URL:
```
https://ваш-проект.up.railway.app
```

---

## Вариант 4: Запуск на своем VPS/сервере

### Требования:
- Ubuntu/Debian сервер
- Python 3.8+
- Доступ по SSH

### Установка:
```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Python и зависимостей
sudo apt install python3 python3-pip -y

# Клонирование проекта
git clone https://github.com/ваш-username/ваш-репозиторий.git
cd ваш-репозиторий

# Установка зависимостей
pip3 install -r requirements_deploy.txt

# Запуск
python3 app.py
```

### Запуск в фоне (с помощью screen):
```bash
screen -S telegram-bot
python3 app.py
# Нажмите Ctrl+A, затем D для выхода
```

### Или с помощью systemd:
Создайте файл `/etc/systemd/system/telegram-bot.service`:
```ini
[Unit]
Description=Telegram Bot Admin Panel
After=network.target

[Service]
Type=simple
User=your-user
WorkingDirectory=/path/to/your/project
ExecStart=/usr/bin/python3 /path/to/your/project/app.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Запуск:
```bash
sudo systemctl enable telegram-bot
sudo systemctl start telegram-bot
sudo systemctl status telegram-bot
```

---

## 🔒 Безопасность

### После развертывания обязательно:

1. **Смените пароль** в файле `web/app.py`:
```python
ADMIN_CREDENTIALS = {
    'username': 'ваш_новый_логин',
    'password': 'ваш_сложный_пароль'
}
```

2. **Смените секретный ключ** в `web/app.py`:
```python
app.secret_key = 'ваш-уникальный-секретный-ключ'
```

3. **Используйте HTTPS** (Render и Railway предоставляют автоматически)

---

## 📱 Проверка работы

1. Откройте ваш URL в браузере
2. Введите логин и пароль
3. Вы должны попасть в админ-панель
4. Проверьте работу бота в Telegram: @createboobs_bot

---

## 🆘 Решение проблем

### Бот не отвечает:
- Проверьте, что токен бота правильный в `bot/config.py`
- Убедитесь, что сервер запущен

### Не могу войти:
- Проверьте логин и пароль
- Очистите cookies браузера

### Ошибка 500:
- Проверьте логи на хостинге
- Убедитесь, что все зависимости установлены

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи на хостинге
2. Убедитесь, что все файлы загружены
3. Проверьте, что база данных создана (папка `data/`)

Удачи с развертыванием! 🚀