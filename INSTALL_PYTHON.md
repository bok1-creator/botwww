# Инструкция по установке Python версии

## 1. Установка Python

### Windows:
1. Перейдите на https://python.org/downloads/
2. Скачайте последнюю версию Python 3.8+
3. **ВАЖНО:** При установке поставьте галочку "Add Python to PATH"
4. Запустите установщик и следуйте инструкциям

### Проверка установки:
```bash
python --version
# или
py --version
```

Должно показать версию Python 3.8 или выше.

## 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

Если возникают ошибки, попробуйте:
```bash
# Обновите pip
python -m pip install --upgrade pip

# Установите зависимости
pip install -r requirements.txt
```

## 3. Запуск бота

```bash
python main.py
```

Вы должны увидеть:
```
🚀 Запуск Telegram Bot Constructor...
🌐 Веб-админка запущена: http://localhost:3000
🤖 Telegram бот запущен!
📱 Bot username: @your_bot_name
```

## 4. Доступ к админ-панели

Откройте в браузере:
```
http://localhost:3000
```

## 5. Тестирование бота

1. Найдите вашего бота в Telegram
2. Отправьте команду `/start`
3. Следуйте инструкциям бота

## 6. Структура файлов после установки

```
TGBOT/
├── bot/                 # Код бота
│   ├── config.py       # Настройки
│   ├── database.py     # База данных
│   ├── handlers.py     # Обработчики
│   └── bot_main.py     # Основной код
├── web/                # Веб-интерфейс
│   └── app.py         # Flask приложение
├── data/              # База данных
│   └── bot.db         # SQLite файл
├── static/            # Статические файлы
│   └── uploads/       # Загруженные видео
├── main.py            # Запуск
├── requirements.txt   # Зависимости
└── README.md         # Документация
```

## 7. Возможные проблемы

### "python не найден"
- Переустановите Python с галочкой "Add to PATH"
- Перезагрузите командную строку
- Попробуйте `py` вместо `python`

### Ошибки при установке пакетов
```bash
# Попробуйте с правами администратора
pip install --user -r requirements.txt

# Или обновите pip
python -m pip install --upgrade pip
```

### Порт 3000 занят
Измените порт в `bot/config.py`:
```python
WEB_PORT = 3001  # или другой свободный порт
```

### Бот не отвечает
- Проверьте токен в `bot/config.py`
- Убедитесь, что интернет работает
- Проверьте, что бот не заблокирован

## 8. Остановка бота

Нажмите `Ctrl+C` в командной строке для остановки.

## 9. Автозапуск (опционально)

### Windows - создайте bat файл:
```batch
@echo off
cd /d "C:\path\to\your\TGBOT"
python main.py
pause
```

### Linux/Mac - создайте скрипт:
```bash
#!/bin/bash
cd /path/to/your/TGBOT
python3 main.py
```

## 10. Логи и отладка

Все логи выводятся в консоль. Для сохранения в файл:
```bash
python main.py > bot.log 2>&1
```

Готово! Ваш бот должен работать. 🎉